# calculate-factorial
This C program calculates the factorial of a number based on user input. It repeatedly prompts the user to enter a number and displays its factorial. The program allows the user to perform multiple calculations by asking if they want to continue. It demonstrates the usage of loops, user input, and factorial computation in C.
